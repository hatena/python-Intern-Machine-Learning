from intern_ml.data import Label
from typing import List


def accuracy_score(gold_labels: List[Label], pred_labels: List[Label]) -> float:
    return 0.0


def precision_score(gold_labels: List[Label], pred_labels: List[Label]) -> float:
    return 0.0


def recall_score(gold_labels: List[Label], pred_labels: List[Label]) -> float:
    return 0.0


def f1_score(gold_labels: List[Label], pred_labels: List[Label]) -> float:
    return 0.0
